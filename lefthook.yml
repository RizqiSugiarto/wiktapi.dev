pre-commit:
  parallel: true
  commands:
    fmt:
      run: pnpm exec vp fmt --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
    lint:
      glob: "*.{js,jsx,ts,tsx,mjs,cjs}"
      run: pnpm exec vp lint --type-aware --type-check {staged_files}
